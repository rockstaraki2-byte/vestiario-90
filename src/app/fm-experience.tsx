"use client";

import Image from "next/image";
import { useEffect, useMemo, useState, type LucideIcon } from "react";
import { Activity, BarChart3, Bell, BriefcaseBusiness, Building2, CalendarDays, ChevronRight, CircleGauge, ClipboardList, Crown, Flag, Globe2, Home, Inbox, Landmark, LayoutGrid, MessageSquareText, Newspaper, Search, Settings, Shield, Sparkles, Target, Trophy, UserRoundSearch, Users, X } from "lucide-react";
import { professionalCompetitionById } from "@/data/brazil-2026/competitions";
import type { GamePreferences } from "@/game-engine/game-preferences";
import { sortedStandings, type LeagueClub, type LeaguePlayer } from "@/game-engine/league";
import { dressingRoomSummary } from "@/game-engine/people";
import { getSelectedClub, getUserFixtures, type SeasonState } from "@/game-engine/season";
import { playerInfluence } from "@/game-engine/social";
import styles from "./fm-experience.module.css";

export type FmEntity={type:"player"|"club"|"competition";id:string};
export const FM_ENTITY_EVENT="v90-fm-open-entity";
export function openFmEntity(entity:FmEntity){if(typeof window!=="undefined")window.dispatchEvent(new CustomEvent<FmEntity>(FM_ENTITY_EVENT,{detail:entity}));}

type NavItem={label:string;icon:LucideIcon};
type Group={label:string;icon:LucideIcon;items:NavItem[]};
export const FM_GROUPS:Group[]=[
 {label:"Início",icon:Home,items:[{label:"Visão geral",icon:Home},{label:"Caixa de entrada",icon:Inbox},{label:"Calendário",icon:CalendarDays},{label:"Notícias",icon:Newspaper}]},
 {label:"Elenco",icon:Users,items:[{label:"Elenco",icon:Users},{label:"Planejamento",icon:ClipboardList},{label:"Vestiário",icon:MessageSquareText},{label:"Central de Dados",icon:BarChart3}]},
 {label:"Jogo",icon:LayoutGrid,items:[{label:"Táticas",icon:LayoutGrid},{label:"Classificação",icon:Trophy}]},
 {label:"Recrutamento",icon:UserRoundSearch,items:[{label:"Mercado",icon:UserRoundSearch}]},
 {label:"Clube",icon:Building2,items:[{label:"Clube",icon:Building2},{label:"Diretoria",icon:Landmark},{label:"Responsabilidades",icon:Shield},{label:"Gestão & Legado",icon:Crown}]},
 {label:"Mundo",icon:Globe2,items:[{label:"Mundo",icon:Globe2},{label:"Seleção",icon:Flag},{label:"Carreira",icon:BriefcaseBusiness},{label:"Mídia & Redes",icon:Bell},{label:"Configurações",icon:Settings}]},
];
export const FM_MOBILE_PRIMARY=["Visão geral","Elenco","Táticas","Calendário"] as const;

export function FmSidebarNav({active,onNavigate,pendingEvents=0,pendingMedia=0}:{active:string;onNavigate:(screen:string)=>void;pendingEvents?:number;pendingMedia?:number}){
 const owner=FM_GROUPS.find(group=>group.items.some(item=>item.label===active))?.label??"Início";
 const[group,setGroup]=useState(owner);
 useEffect(()=>{setGroup(owner)},[owner]);
 const selected=FM_GROUPS.find(item=>item.label===group)??FM_GROUPS[0];
 return <div className={styles.navShell}><div className={styles.primaryNav}>{FM_GROUPS.map(item=>{const Icon=item.icon,hot=item.items.some(child=>child.label===active);return <button key={item.label} className={hot?styles.primaryActive:""} onClick={()=>setGroup(item.label)}><Icon size={17}/><span>{item.label}</span></button>})}</div><div className={styles.secondaryNav}><small>{selected.label.toUpperCase()}</small>{selected.items.map(item=>{const Icon=item.icon,count=item.label==="Caixa de entrada"?pendingEvents:item.label==="Mídia & Redes"?pendingMedia:0;return <button key={item.label} className={active===item.label?styles.secondaryActive:""} onClick={()=>onNavigate(item.label)}><Icon size={16}/><span>{item.label}</span>{count>0&&<i>{Math.min(99,count)}</i>}</button>})}</div></div>;
}

export function FmMobileMenu({active,onNavigate,pendingEvents=0,pendingMedia=0,onClose}:{active:string;onNavigate:(screen:string)=>void;pendingEvents?:number;pendingMedia?:number;onClose:()=>void}){
 return <div className={styles.mobileGroups}>{FM_GROUPS.map(group=>{const GroupIcon=group.icon;return <section key={group.label}><header><GroupIcon size={15}/><b>{group.label}</b></header><div>{group.items.filter(item=>!FM_MOBILE_PRIMARY.includes(item.label as typeof FM_MOBILE_PRIMARY[number])).map(item=>{const Icon=item.icon,count=item.label==="Caixa de entrada"?pendingEvents:item.label==="Mídia & Redes"?pendingMedia:0;return <button key={item.label} className={active===item.label?styles.mobileGroupActive:""} onClick={()=>{onNavigate(item.label);onClose()}}><Icon size={17}/><span>{item.label}</span>{count>0&&<i>{Math.min(99,count)}</i>}</button>})}</div></section>})}</div>;
}

function groupFor(active:string){return FM_GROUPS.find(group=>group.items.some(item=>item.label===active))?.label??"Início";}

type SearchResult={id:string;kind:string;title:string;detail:string;screen?:string;entity?:FmEntity};
function searchResults(season:SeasonState,query:string):SearchResult[]{
 const q=query.trim().toLocaleLowerCase("pt-BR"),competition=professionalCompetitionById(season.competitionId);
 const screens=FM_GROUPS.flatMap(group=>group.items.map(item=>({id:item.label,kind:"ÁREA",title:item.label,detail:`${group.label} • abrir módulo`,screen:item.label})));
 const clubs=season.league.clubs.map(club=>({id:club.id,kind:"CLUBE",title:club.name,detail:`${competition.shortName} • reputação ${club.reputation}`,entity:{type:"club",id:club.id} as FmEntity}));
 const players=season.league.clubs.flatMap(club=>club.players.map(player=>({id:player.id,kind:"JOGADOR",title:player.name,detail:`${club.shortName} • ${player.position} • ${player.age} anos • OVR ${player.overall}`,entity:{type:"player",id:player.id} as FmEntity})));
 const all:SearchResult[]=[{id:season.competitionId,kind:"COMPETIÇÃO",title:competition.name,detail:`Temporada ${season.year} • ${season.league.clubs.length} clubes`,entity:{type:"competition",id:season.competitionId}},...screens,...clubs,...players];
 if(!q)return all.slice(0,14);return all.filter(item=>`${item.title} ${item.detail} ${item.kind}`.toLocaleLowerCase("pt-BR").includes(q)).sort((a,b)=>searchRank(a,q)-searchRank(b,q)||a.title.localeCompare(b.title));
}
function searchRank(item:SearchResult,q:string){const title=item.title.toLocaleLowerCase("pt-BR");return title===q?0:title.startsWith(q)?1:title.includes(q)?2:3;}

export function FmContextBar({season,active,onNavigate}:{season:SeasonState;active:string;onNavigate:(screen:string)=>void}){
 const[searchOpen,setSearchOpen]=useState(false),[query,setQuery]=useState(""),[entity,setEntity]=useState<FmEntity|null>(null),club=getSelectedClub(season),competition=professionalCompetitionById(season.competitionId),advice=assistantAdvice(season),results=useMemo(()=>searchResults(season,query),[season,query]);
 useEffect(()=>{const listener=(event:Event)=>setEntity((event as CustomEvent<FmEntity>).detail);window.addEventListener(FM_ENTITY_EVENT,listener);return()=>window.removeEventListener(FM_ENTITY_EVENT,listener)},[]);
 useEffect(()=>{const key=(event:KeyboardEvent)=>{if((event.ctrlKey||event.metaKey)&&event.key.toLowerCase()==="k"){event.preventDefault();setSearchOpen(true)}if(event.key==="Escape"){setSearchOpen(false);setEntity(null)}};window.addEventListener("keydown",key);return()=>window.removeEventListener("keydown",key)},[]);
 return <><div className={styles.contextBar}><div className={styles.breadcrumb}><button onClick={()=>onNavigate("Visão geral")}>V90</button><ChevronRight/><span>{groupFor(active)}</span><ChevronRight/><b>{active}</b></div><button className={styles.assistantChip} onClick={()=>onNavigate(advice.target)}><Sparkles/><span><small>ADJUNTO</small><b>{advice.title}</b></span><ChevronRight/></button><button className={styles.searchButton} onClick={()=>setSearchOpen(true)}><Search/><span>Pesquisar no mundo</span><kbd>Ctrl K</kbd></button></div>{searchOpen&&<div className={styles.overlay} onClick={()=>setSearchOpen(false)}><section className={styles.searchPanel} onClick={event=>event.stopPropagation()}><header><Search/><input autoFocus value={query} onChange={e=>setQuery(e.target.value)} placeholder="Jogador, clube, competição ou área do jogo..."/><button onClick={()=>setSearchOpen(false)}><X/></button></header><div className={styles.searchMeta}><span>{club.name}</span><span>{competition.shortName}</span><span>{results.length} resultados</span></div><div className={styles.searchResults}>{results.slice(0,18).map(result=><button key={`${result.kind}-${result.id}`} onClick={()=>{if(result.screen){onNavigate(result.screen);setSearchOpen(false)}else if(result.entity){setEntity(result.entity);setSearchOpen(false)}}}><i>{result.kind}</i><span><b>{result.title}</b><small>{result.detail}</small></span><ChevronRight/></button>)}</div></section></div>}{entity&&<EntityProfile season={season} entity={entity} onClose={()=>setEntity(null)} onNavigate={onNavigate}/>}</>;
}

export function ManagerPortal({season,onNavigate}:{season:SeasonState;onNavigate:(screen:string)=>void}){
 const club=getSelectedClub(season),standings=sortedStandings(season.league),row=standings.find(s=>s.clubId===club.id),position=Math.max(1,standings.findIndex(s=>s.clubId===club.id)+1),pending=season.livingWorld.inbox.filter(event=>!event.resolved),summary=dressingRoomSummary(club,season.currentRound),advice=assistantAdvice(season),next=getUserFixtures(season).find(f=>!f.played&&Boolean(f.date)&&f.date!>=season.currentDate),opponent=next?season.league.clubs.find(c=>c.id===(next.homeClubId===club.id?next.awayClubId:next.homeClubId)):undefined;
 return <div className={styles.portal}><section className={styles.portalHero}><div><span>PORTAL DO TREINADOR • {formatLongDate(season.currentDate)}</span><h2>{dayGreeting(season.currentDate)}, treinador.</h2><p>{pending.length?`${pending.length} assunto${pending.length===1?" exige":"s exigem"} sua atenção antes de avançar.`:"Não há decisões obrigatórias neste momento. Prepare o próximo passo com calendário, staff e dados."}</p></div><button onClick={()=>onNavigate(advice.target)}><Sparkles/><span><small>CONSELHO DA COMISSÃO</small><b>{advice.title}</b><em>{advice.detail}</em></span><ChevronRight/></button></section><TwoWeekCalendar season={season} onNavigate={onNavigate}/><div className={styles.portalGrid}><section className={`${styles.fmCard} ${styles.decisionCard}`}><header><span>VOCÊ PRECISA DECIDIR</span><button onClick={()=>onNavigate("Caixa de entrada")}>Ver tarefas <ChevronRight/></button></header>{pending.length?pending.slice(0,4).map((event,index)=><button key={event.id} className={index===0?styles.priorityDecision:""} onClick={()=>onNavigate(event.kind==="Diretoria"?"Diretoria":event.kind==="Coletiva"?"Mídia & Redes":"Caixa de entrada")}><i>{event.kind.slice(0,3).toUpperCase()}</i><span><b>{event.title}</b><small>{event.body}</small></span><em>{index===0?"PRIORIDADE":`R${event.round}`}</em></button>):<div className={styles.emptyTile}><Shield/><b>Agenda limpa</b><small>Sem decisões pendentes.</small></div>}</section><section className={styles.fmCard}><header><span>PRÓXIMO JOGO</span><button onClick={()=>onNavigate("Táticas")}>Preparar <ChevronRight/></button></header>{next&&opponent?<div className={styles.nextGame}><button onClick={()=>openFmEntity({type:"club",id:club.id})}><Image src={club.imageUrl} alt={club.name} width={46} height={46}/><b>{club.shortName}</b></button><div><small>{formatCompactDate(next.date)}</small><strong>VS</strong><em>{next.homeClubId===club.id?"CASA":"FORA"}</em></div><button onClick={()=>openFmEntity({type:"club",id:opponent.id})}><Image src={opponent.imageUrl} alt={opponent.name} width={46} height={46}/><b>{opponent.shortName}</b></button></div>:<div className={styles.emptyTile}><CalendarDays/><b>Sem jogo marcado</b></div>}</section><section className={styles.fmCard}><header><span>STATUS DO TRABALHO</span><button onClick={()=>onNavigate("Diretoria")}>Diretoria <ChevronRight/></button></header><div className={styles.statusGrid}><Metric label="LIGA" value={`${position}º`} detail={row?`${row.points} pts • ${row.played} J`:"—"}/><Metric label="DIRETORIA" value={`${season.livingWorld.boardConfidence}%`} detail={season.boardState.mandate.name}/><Metric label="VESTIÁRIO" value={`${Math.round((summary.avgHappiness+summary.avgTrust+summary.unity)/3)}%`} detail={`${summary.concerns.length} questões`}/><Metric label="FORMA" value={season.recentForm.slice(-5).join(" ")||"—"} detail="últimos jogos"/></div></section><section className={styles.fmCard}><header><span>HOJE NO FUTEBOL</span><button onClick={()=>onNavigate("Notícias")}>Central <ChevronRight/></button></header><div className={styles.newsStack}>{season.livingWorld.news.slice(0,4).map((item,index)=><article key={item.id} className={index===0?styles.leadNews:""}><small>{item.source}</small><b>{item.headline}</b><p>{item.summary}</p></article>)}</div></section></div></div>;
}

function TwoWeekCalendar({season,onNavigate}:{season:SeasonState;onNavigate:(screen:string)=>void}){const club=getSelectedClub(season),fixtures=getUserFixtures(season),days=Array.from({length:14},(_,index)=>addDays(season.currentDate,index));return <section className={styles.fortnight}><header><div><span>PRÓXIMAS DUAS SEMANAS</span><b>Jogos, treino e recuperação</b></div><button onClick={()=>onNavigate("Calendário")}>Calendário completo <ChevronRight/></button></header><div>{days.map((date,index)=>{const fixture=fixtures.find(item=>!item.played&&item.date===date),opp=fixture?season.league.clubs.find(c=>c.id===(fixture.homeClubId===club.id?fixture.awayClubId:fixture.homeClubId)):undefined,label=fixture?`Jogo • ${opp?.shortName??"Adversário"}`:index%7===0?"Recuperação":season.trainingPlan;return <button key={date} className={`${date===season.currentDate?styles.todayDay:""} ${fixture?styles.matchDay:""}`} onClick={()=>onNavigate(fixture?"Táticas":"Calendário")}><small>{weekday(date)}</small><b>{date.slice(8)}</b><span>{label}</span></button>})}</div></section>}

export function FmSpecialView({active,season,onNavigate,onPreferencesChange}:{active:string;season:SeasonState;onNavigate:(screen:string)=>void;onPreferencesChange:(preferences:GamePreferences)=>void}){if(active==="Planejamento")return <SquadPlanner season={season} onNavigate={onNavigate}/>;if(active==="Diretoria")return <BoardConfidence season={season} onNavigate={onNavigate}/>;if(active==="Responsabilidades")return <ResponsibilitiesHub season={season} onChange={onPreferencesChange}/>;return null;}

export function DataHubDecisionStrip({season,onNavigate}:{season:SeasonState;onNavigate:(screen:string)=>void}){const club=getSelectedClub(season),players=club.players,avgCondition=Math.round(players.reduce((s,p)=>s+p.condition,0)/Math.max(1,players.length)),avgFatigue=Math.round(players.reduce((s,p)=>s+p.fatigue,0)/Math.max(1,players.length)),lowMood=players.filter(p=>p.happiness<55||p.managerTrust<48).length,goals=players.reduce((s,p)=>s+p.goals,0),shots=players.reduce((s,p)=>s+p.shots,0),conversion=shots?Math.round(goals/shots*100):0,insights=[{warn:avgFatigue>45,title:avgFatigue>45?"Carga física acima do ideal":"Carga física controlada",detail:`Condição ${avgCondition}% • fadiga ${avgFatigue}%.`,action:"Elenco",target:"Elenco"},{warn:lowMood>=4,title:lowMood>=4?`${lowMood} jogadores em zona de atenção":"Vestiário sem alerta coletivo",detail:"Confiança, satisfação e minutos afetam o grupo.",action:"Vestiário",target:"Vestiário"},{warn:conversion<9,title:conversion<9?"Eficiência ofensiva pede revisão":"Ataque em faixa aceitável",detail:`${goals} gols • ${shots} chutes • ${conversion}% conversão.`,action:"Táticas",target:"Táticas"}];return <section className={styles.dataDecisions}><header><Activity/><div><span>DATA HUB • DECISÕES</span><b>Transforme números em ações</b></div></header><div>{insights.map(item=><button key={item.title} className={item.warn?styles.insightWarn:""} onClick={()=>onNavigate(item.target)}><span><b>{item.title}</b><small>{item.detail}</small></span><em>{item.action} <ChevronRight/></em></button>)}</div></section>}

function SquadPlanner({season,onNavigate}:{season:SeasonState;onNavigate:(screen:string)=>void}){const club=getSelectedClub(season),slots=["GOL","LD","ZAG","LE","VOL","MC","MEI","PD/PE","ATA"],groups=slots.map(slot=>({slot,players:club.players.filter(player=>plannerSlot(player.position)===slot).sort((a,b)=>b.overall-a.overall)}));return <div className={styles.workspace}><section className={styles.workspaceHero}><div><span>PLANEJAMENTO DE ELENCO</span><h2>Profundidade, potencial e necessidades por setor</h2><p>Veja titular, cobertura e carências antes de ir ao mercado.</p></div><ClipboardList/></section><div className={styles.plannerGrid}>{groups.map(group=>{const first=group.players[0],depth=group.players.length,need=!first?"URGENTE":depth<2||(first?.overall??0)<64?"ALTA":depth<3?"MÉDIA":"BAIXA";return <section className={styles.positionCard} key={group.slot}><header><b>{group.slot}</b><span className={need==="URGENTE"||need==="ALTA"?styles.needHigh:""}>NECESSIDADE {need}</span></header><div>{group.players.slice(0,4).map((player,index)=><button key={player.id} onClick={()=>openFmEntity({type:"player",id:player.id})}><i>{index+1}</i><span><b>{player.name}</b><small>{player.age} anos • OVR {player.overall} • POT {player.potential}</small></span><em>{index===0?"Titular":index===1?"Cobertura":player.squadRole}</em></button>)}</div>{depth<3&&<button className={styles.marketNeed} onClick={()=>onNavigate("Mercado")}><UserRoundSearch/> Encontrar opção <ChevronRight/></button>}</section>})}</div></div>}

function BoardConfidence({season,onNavigate}:{season:SeasonState;onNavigate:(screen:string)=>void}){const board=season.boardState,club=getSelectedClub(season),standings=sortedStandings(season.league),position=standings.findIndex(s=>s.clubId===club.id)+1,avgMood=Math.round(club.players.reduce((s,p)=>s+p.morale,0)/Math.max(1,club.players.length)),wage=club.players.reduce((s,p)=>s+p.contract.salaryBrlMonthly,0),finance=Math.round(Math.min(100,club.wageBudgetBrlMonthly/Math.max(1,wage)*80)),last=board.evaluations?.[0],grades=[{label:"Resultados",score:Math.max(0,Math.min(100,78-(position-board.targetPosition)*10))},{label:"Vestiário",score:avgMood},{label:"Finanças",score:finance},{label:"Base",score:board.objectives.find(o=>o.id==="youth")?.progress??50},{label:"Mercado",score:club.transferBudgetEur>0?72:52}];return <div className={styles.workspace}><section className={styles.boardHero}><div><span>CONFIANÇA DA DIRETORIA</span><h2>{season.livingWorld.boardConfidence}% • {last?.verdict??"Avaliação em andamento"}</h2><p>{board.mandate.description}</p></div><CircleGauge/></section><div className={styles.boardGrid}><section className={styles.fmCard}><header><span>MANDATO</span><b>{board.mandate.name}</b></header><dl><div><dt>Meta na liga</dt><dd>Top {board.targetPosition}</dd></div><div><dt>Pressão</dt><dd>{board.mandate.pressure}</dd></div><div><dt>Paciência</dt><dd>{board.mandate.patience}/100</dd></div></dl><div className={styles.nonNegotiables}>{board.mandate.nonNegotiables.map(item=><span key={item}><Target/> {item}</span>)}</div></section><section className={styles.fmCard}><header><span>AVALIAÇÃO POR ÁREA</span><button onClick={()=>onNavigate("Clube")}>Gestão <ChevronRight/></button></header><div className={styles.gradeList}>{grades.map(item=><div key={item.label}><span><b>{item.label}</b><em>{grade(item.score)}</em></span><i><b style={{width:`${Math.max(0,Math.min(100,item.score))}%`}}/></i></div>)}</div></section></div><section className={styles.fmCard}><header><span>OBJETIVOS DA TEMPORADA</span><b>{board.objectives.length}</b></header><div className={styles.objectiveGrid}>{board.objectives.map(objective=><article key={objective.id}><span>{objective.category} • {objective.priority}</span><b>{objective.title}</b><p>{objective.description}</p><div><i><b style={{width:`${objective.progress}%`}}/></i><em>{objective.progress}% • {objective.status}</em></div></article>)}</div></section>{last&&<section className={styles.fmCard}><header><span>ÚLTIMA AVALIAÇÃO</span><b>Rodada {last.round}</b></header><p className={styles.boardSummary}>{last.summary}</p></section>}</div>}

function ResponsibilitiesHub({season,onChange}:{season:SeasonState;onChange:(preferences:GamePreferences)=>void}){const prefs=season.preferences,rows:[keyof GamePreferences["responsibilities"],string,string[]][]=[["matchSelection","Escalação e banco",["Treinador","Adjunto"]],["training","Planejamento de treino",["Treinador","Adjunto"]],["pressConferences","Coletivas de imprensa",["Treinador","Adjunto"]],["teamTalks","Palestras ao elenco",["Treinador","Adjunto"]],["transferTargets","Alvos de contratação",["Treinador","Diretor de futebol"]],["contractRenewals","Prioridades de renovação",["Treinador","Diretor de futebol"]],["outgoingTransfers","Saídas e empréstimos",["Treinador","Diretor de futebol"]],["scouting","Scouting e relatórios",["Treinador","Diretor de futebol"]],["youthDevelopment","Desenvolvimento da base",["Treinador","Responsável pela base"]]];const change=(key:keyof GamePreferences["responsibilities"],value:string)=>onChange({...prefs,responsibilities:{...prefs.responsibilities,[key]:value} as GamePreferences["responsibilities"]});return <div className={styles.workspace}><section className={styles.workspaceHero}><div><span>RESPONSABILIDADES</span><h2>O que é seu e o que fica com o staff</h2><p>Delegue rotina sem perder a decisão estratégica.</p></div><Shield/></section><section className={styles.responsibilityBoard}><header><b>VOCÊ</b><span>ÁREA</span><b>STAFF / DIRETORIA</b></header>{rows.map(([key,label,options])=>{const value=String(prefs.responsibilities[key]);return <article key={key}><div className={value==="Treinador"?styles.ownerActive:""}>Treinador</div><span><b>{label}</b><small>{responsibilityDetail(key)}</small></span><select value={value} onChange={e=>change(key,e.target.value)}>{options.map(option=><option key={option}>{option}</option>)}</select></article>})}</section></div>}

function EntityProfile({season,entity,onClose,onNavigate}:{season:SeasonState;entity:FmEntity;onClose:()=>void;onNavigate:(screen:string)=>void}){const[tab,setTab]=useState("Visão geral"),found=findPlayer(season,entity.id),club=entity.type==="club"?season.league.clubs.find(c=>c.id===entity.id):found?.club,player=entity.type==="player"?found?.player:undefined,competition=professionalCompetitionById(season.competitionId),tabs=player?["Visão geral","Atributos","Desempenho","Desenvolvimento","Contrato","Felicidade","Relações","Histórico"]:club?["Visão geral","Elenco","Resultados","Finanças"]:["Visão geral","Classificação","Estatísticas"];useEffect(()=>{setTab("Visão geral")},[entity.type,entity.id]);return <div className={styles.entityBackdrop} onClick={onClose}><section className={styles.entitySheet} onClick={event=>event.stopPropagation()}><header className={styles.entityTop}><div>{player?<div className={styles.entityAvatar}>{initials(player.name)}</div>:club?<Image src={club.imageUrl} alt={club.name} width={58} height={58}/>:<Trophy size={48}/>}<span><small>{player?`${club?.name} • ${player.position}`:club?competition.name:`COMPETIÇÃO • ${season.year}`}</small><h2>{player?.name??club?.name??competition.name}</h2><p>{player?`${player.age} anos • ${player.nationality??"—"} • ${player.squadRole}`:club?`Reputação ${club.reputation} • ${club.players.length} jogadores`:`${season.league.clubs.length} clubes`}</p></span></div><button onClick={onClose}><X/></button></header><nav className={styles.entityTabs}>{tabs.map(item=><button key={item} className={tab===item?styles.entityTabActive:""} onClick={()=>setTab(item)}>{item}</button>)}</nav><div className={styles.entityContent}>{player&&club?<PlayerProfile player={player} club={club} tab={tab} onNavigate={onNavigate}/>:club?<ClubProfile season={season} club={club} tab={tab}/>:<CompetitionProfile season={season} tab={tab}/>}</div></section></div>}

function PlayerProfile({player,club,tab,onNavigate}:{player:LeaguePlayer;club:LeagueClub;tab:string;onNavigate:(screen:string)=>void}){const influence=playerInfluence(player);if(tab==="Atributos")return <div className={styles.attributeGrid}>{attributeGroups(player).map(group=><section key={group.name}><header>{group.name}</header>{group.items.map(item=><div key={item.label}><span>{item.label}</span><b>{item.value}</b><i><em style={{width:`${item.value}%`}}/></i></div>)}</section>)}</div>;if(tab==="Desempenho")return <div className={styles.entityStats}><Metric label="JOGOS" value={String(player.appearances)} detail={`${player.starts} titular`}/><Metric label="MINUTOS" value={String(player.minutes)} detail={`${player.goals} G • ${player.assists} A`}/><Metric label="NOTA" value={player.ratedMatches?player.averageRating.toFixed(2):"—"} detail="média"/><Metric label="FORMA" value={`${player.form}/10`} detail={`${player.shots} chutes`}/></div>;if(tab==="Desenvolvimento")return <InfoColumns leftTitle="DESENVOLVIMENTO" leftHead={`OVR ${player.overall} → POT ${player.potential}`} leftText={`Foco: ${player.individualTrainingFocus??"Equilibrado"}. Progresso acumulado ${Math.round(player.developmentProgress*100)}%.`} rightTitle="PLANO" rightHead={player.retrainingPosition?`Adaptação: ${player.retrainingPosition}`:"Posição consolidada"} rightText={player.mentorPlayerId?"Mentoring ativo.":"Sem mentor definido."}/>;if(tab==="Contrato")return <InfoColumns leftTitle="CONTRATO" leftHead={`Até ${player.contract.endYear}`} leftText={`${brl(player.contract.salaryBrlMonthly)}/mês • agente ${player.contract.agentName}.`} rightTitle="MERCADO" rightHead={player.marketValueEur?eur(player.marketValueEur):"Sem valor"} rightText={player.transferListed?"Listado para transferência.":player.wantsToLeave?"O jogador demonstra desejo de sair.":"Sem intenção de saída."}/>;if(tab==="Felicidade")return <div className={styles.entityStats}><Metric label="FELICIDADE" value={`${player.happiness}%`} detail="satisfação"/><Metric label="CONFIANÇA" value={`${player.managerTrust}%`} detail="no treinador"/><Metric label="MORAL" value={`${player.morale}%`} detail={player.personality}/><Metric label="INFLUÊNCIA" value={`${influence}%`} detail={player.squadRole}/></div>;if(tab==="Relações")return <InfoColumns leftTitle="HIERARQUIA" leftHead={`${influence}/100 influência`} leftText={`${player.squadRole} • ${player.personality}. Decisões sobre ele podem repercutir conforme sua posição social.`} rightTitle="VESTIÁRIO" rightHead={`${player.managerTrust}% confiança`} rightText={player.happiness<55?"Há insatisfação que merece atenção.":"Relação individual está controlada."}/>;if(tab==="Histórico")return <InfoColumns leftTitle="CARREIRA NO SAVE" leftHead={`${player.appearances} jogos`} leftText={`${player.goals} gols • ${player.assists} assistências • no clube desde ${player.joinedClubYear??"o início do save"}.`} rightTitle="PROMESSAS" rightHead={`${player.promises?.length??0} registradas`} rightText={player.promises?.find(p=>p.status==="Ativa")?"Existe uma promessa ativa.":"Nenhuma promessa ativa."}/>;return <div className={styles.entityOverview}><div className={styles.entityStats}><Metric label="OVR" value={String(player.overall)} detail={`POT ${player.potential}`}/><Metric label="CONDIÇÃO" value={`${player.condition}%`} detail={`fadiga ${player.fatigue}%`}/><Metric label="MORAL" value={`${player.morale}%`} detail={player.status}/><Metric label="VALOR" value={player.marketValueEur?eur(player.marketValueEur):"—"} detail={player.contract.agentName}/></div><section className={styles.profileNarrative}><span>RESUMO DO STAFF</span><h3>{player.squadRole} • {player.personality}</h3><p>{player.potential-player.overall>=6?"Ainda existe margem importante de evolução.":"É uma opção estabilizada no nível atual."} {player.happiness<55?"O jogador precisa de atenção humana.":"O ambiente individual está controlado."}</p><div><button onClick={()=>openFmEntity({type:"club",id:club.id})}>Ver {club.name}</button><button onClick={()=>onNavigate("Planejamento")}>Ver no Planner</button></div></section></div>}

function ClubProfile({season,club,tab}:{season:SeasonState;club:LeagueClub;tab:string}){const table=sortedStandings(season.league),standing=table.find(s=>s.clubId===club.id),position=table.findIndex(s=>s.clubId===club.id)+1,top=[...club.players].sort((a,b)=>b.overall-a.overall).slice(0,8);if(tab==="Elenco")return <div className={styles.entityList}>{top.map(player=><button key={player.id} onClick={()=>openFmEntity({type:"player",id:player.id})}><span><b>{player.name}</b><small>{player.position} • {player.age} anos</small></span><strong>{player.overall}</strong><ChevronRight/></button>)}</div>;if(tab==="Finanças")return <div className={styles.entityStats}><Metric label="ELENCO" value={eur(club.marketValueEur)} detail="valor"/><Metric label="TRANSFERÊNCIAS" value={eur(club.transferBudgetEur)} detail="orçamento"/><Metric label="TETO SALARIAL" value={brl(club.wageBudgetBrlMonthly)} detail="mensal"/><Metric label="REPUTAÇÃO" value={String(club.reputation)} detail="0–100"/></div>;if(tab==="Resultados"){const fixtures=season.league.fixtures.filter(f=>f.played&&(f.homeClubId===club.id||f.awayClubId===club.id)).slice(-6).reverse();return <div className={styles.entityList}>{fixtures.map(f=><article key={f.id}><span><b>{season.league.clubs.find(c=>c.id===f.homeClubId)?.shortName} {f.homeGoals} × {f.awayGoals} {season.league.clubs.find(c=>c.id===f.awayClubId)?.shortName}</b><small>Rodada {f.round}</small></span></article>)}</div>}return <div className={styles.entityStats}><Metric label="POSIÇÃO" value={`${position}º`} detail={standing?`${standing.points} pts`:"—"}/><Metric label="ELENCO" value={String(club.players.length)} detail="jogadores"/><Metric label="REPUTAÇÃO" value={String(club.reputation)} detail="força"/><Metric label="MERCADO" value={eur(club.marketValueEur)} detail="valor"/></div>}
function CompetitionProfile({season,tab}:{season:SeasonState;tab:string}){const table=sortedStandings(season.league),played=season.league.fixtures.filter(f=>f.played),goals=played.reduce((s,f)=>s+(f.homeGoals??0)+(f.awayGoals??0),0);if(tab==="Classificação")return <div className={styles.entityList}>{table.slice(0,12).map((row,index)=>{const club=season.league.clubs.find(c=>c.id===row.clubId)!;return <button key={club.id} onClick={()=>openFmEntity({type:"club",id:club.id})}><em>{index+1}</em><span><b>{club.name}</b><small>{row.won}V {row.drawn}E {row.lost}D</small></span><strong>{row.points} pts</strong></button>})}</div>;return <div className={styles.entityStats}><Metric label="CLUBES" value={String(season.league.clubs.length)} detail="participantes"/><Metric label="JOGOS" value={String(played.length)} detail="disputados"/><Metric label="GOLS" value={String(goals)} detail={played.length?`${(goals/played.length).toFixed(2)}/jogo`:"—"}/><Metric label="TEMPORADA" value={String(season.year)} detail={tab}/></div>}
function InfoColumns({leftTitle,leftHead,leftText,rightTitle,rightHead,rightText}:{leftTitle:string;leftHead:string;leftText:string;rightTitle:string;rightHead:string;rightText:string}){return <div className={styles.entityColumns}><section><span>{leftTitle}</span><h3>{leftHead}</h3><p>{leftText}</p></section><section><span>{rightTitle}</span><h3>{rightHead}</h3><p>{rightText}</p></section></div>}

export function DressingRoomHierarchy({season}:{season:SeasonState}){const club=getSelectedClub(season),ordered=[...club.players].sort((a,b)=>playerInfluence(b)-playerInfluence(a)),bands=[{title:"LIDERANÇAS",items:ordered.slice(0,3)},{title:"INFLUENTES",items:ordered.slice(3,8)},{title:"GRUPO PRINCIPAL",items:ordered.slice(8,16)},{title:"PERIFÉRICOS",items:ordered.slice(16)}];return <section className={styles.fmCard}><header><span>HIERARQUIA SOCIAL DO ELENCO</span><b>{club.players.length} atletas</b></header><div className={styles.objectiveGrid}>{bands.map(band=><article key={band.title}><span>{band.title}</span>{band.items.slice(0,6).map(player=><button key={player.id} onClick={()=>openFmEntity({type:"player",id:player.id})}>{player.name}<small>{playerInfluence(player)}%</small></button>)}</article>)}</div></section>}

function assistantAdvice(season:SeasonState){const club=getSelectedClub(season),summary=dressingRoomSummary(club,season.currentRound),fatigue=Math.round(club.players.reduce((s,p)=>s+p.fatigue,0)/Math.max(1,club.players.length)),next=getUserFixtures(season).find(f=>!f.played&&f.date&&f.date>=season.currentDate);if(season.livingWorld.boardConfidence<45)return{title:"A diretoria está aumentando a pressão",detail:"Revise mandato e objetivos.",target:"Diretoria"};if(summary.concerns.length>=4)return{title:"O vestiário pede intervenção",detail:`${summary.concerns.length} atletas em atenção.`,target:"Vestiário"};if(fatigue>=45)return{title:"Considere reduzir a carga",detail:`Fadiga média ${fatigue}%.`,target:"Elenco"};if(next)return{title:"Prepare o próximo adversário",detail:`Jogo em ${formatCompactDate(next.date)}.`,target:"Táticas"};return{title:"Procure a próxima vantagem no Data Hub",detail:"Sem alerta urgente.",target:"Central de Dados"}}
function findPlayer(season:SeasonState,id:string){for(const club of season.league.clubs){const player=club.players.find(item=>item.id===id);if(player)return{club,player}}return undefined}
function plannerSlot(position:string){const p=position.toUpperCase();if(p.includes("GOL"))return"GOL";if(p==="LD"||p.includes("DIREIT"))return"LD";if(p.includes("ZAG"))return"ZAG";if(p==="LE"||p.includes("ESQUERD"))return"LE";if(p.includes("VOL"))return"VOL";if(p==="MC"||p.includes("MEIO-CAMPISTA"))return"MC";if(p.includes("MEI"))return"MEI";if(p.includes("PONTA")||p==="PD"||p==="PE")return"PD/PE";return"ATA"}
function attributeGroups(player:LeaguePlayer){const v=(offset:number)=>Math.max(35,Math.min(99,Math.round(player.overall+offset+(player.form-6)*1.5)));return[{name:"TÉCNICO",items:[{label:"Finalização",value:v(player.goals>3?4:0)},{label:"Passe",value:v(player.assists>3?3:-1)},{label:"Primeiro toque",value:v(1)},{label:"Técnica",value:v(2)}]},{name:"MENTAL",items:[{label:"Decisões",value:v(player.managerTrust>70?3:0)},{label:"Determinação",value:v(player.personality==="Competitivo"?5:1)},{label:"Trabalho em equipe",value:v(player.happiness>70?3:0)},{label:"Compostura",value:v(player.morale>75?4:0)}]},{name:"FÍSICO",items:[{label:"Condição",value:player.condition},{label:"Resistência",value:Math.max(35,100-player.fatigue)},{label:"Ritmo",value:v(player.age<=24?4:player.age>=32?-4:1)},{label:"Força",value:v(player.age>=27?2:0)}]}]}
function Metric({label,value,detail}:{label:string;value:string;detail:string}){return <div className={styles.fmMetric}><span>{label}</span><b>{value}</b><small>{detail}</small></div>}
function responsibilityDetail(key:keyof GamePreferences["responsibilities"]){const map:Record<keyof GamePreferences["responsibilities"],string>={matchSelection:"Escolha dos 11 e banco.",training:"Carga e foco da preparação.",pressConferences:"Coletivas pré e pós-jogo.",teamTalks:"Mensagens ao elenco.",transferTargets:"Prioridades de mercado.",contractRenewals:"Renovações prioritárias.",outgoingTransfers:"Venda, empréstimo e saída.",scouting:"Observação e relatórios.",youthDevelopment:"Evolução das categorias de base."};return map[key]}
function grade(value:number){return value>=85?"A+":value>=76?"A":value>=68?"B":value>=58?"C":value>=45?"D":"E"}
function addDays(iso:string,days:number){const date=new Date(`${iso}T12:00:00Z`);date.setUTCDate(date.getUTCDate()+days);return date.toISOString().slice(0,10)}
function weekday(iso:string){return new Intl.DateTimeFormat("pt-BR",{weekday:"short",timeZone:"UTC"}).format(new Date(`${iso}T12:00:00Z`)).replace(".","").toUpperCase()}
function formatLongDate(iso:string){return new Intl.DateTimeFormat("pt-BR",{weekday:"long",day:"2-digit",month:"long",year:"numeric",timeZone:"UTC"}).format(new Date(`${iso}T12:00:00Z`))}
function formatCompactDate(iso?:string){if(!iso)return"data a definir";return new Intl.DateTimeFormat("pt-BR",{day:"2-digit",month:"short",timeZone:"UTC"}).format(new Date(`${iso}T12:00:00Z`))}
function dayGreeting(iso:string){const day=new Date(`${iso}T12:00:00Z`).getUTCDay();return day===0?"Domingo de futebol":day===6?"Sábado de jogo":"Bom trabalho"}
function initials(name:string){return name.split(" ").slice(0,2).map(part=>part[0]).join("")}
function eur(value:number){return value>=1_000_000?`€ ${(value/1_000_000).toLocaleString("pt-BR",{maximumFractionDigits:1})} mi`:`€ ${Math.round(value/1_000).toLocaleString("pt-BR")} mil`}
function brl(value:number){return new Intl.NumberFormat("pt-BR",{style:"currency",currency:"BRL",maximumFractionDigits:0}).format(value)}
