import{describe,expect,it}from"vitest";import{SeededRng}from"./rng";import{advanceDay,clamp,createInitialWorld}from"./world";describe("engine determinística",()=>{it("repete a sequência com a mesma seed",()=>{const a=new SeededRng("save-1"),b=new SeededRng("save-1");expect([a.next(),a.next()]).toEqual([b.next(),b.next()])});it("mantém moral nos limites",()=>{expect(clamp(120,0,100)).toBe(100);expect(clamp(-5,0,100)).toBe(0)});it("avança o mundo de forma reproduzível",()=>{const a=advanceDay(createInitialWorld("carreira")),b=advanceDay(createInitialWorld("carreira"));expect(a).toEqual(b);expect(a.day).toBe(18)})});
