name: Finalize Sprint 12
on:
  push:
    branches: [feat/sprint12-multisave-calendar-leagues]
    paths:
      - scripts/finalize_sprint12.py
      - .github/workflows/apply-sprint12.yml
permissions:
  contents: write
jobs:
  finalize:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/sprint12-multisave-calendar-leagues
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - name: Apply Sprint 12 to current sources
        run: python scripts/finalize_sprint12.py
      - run: npm ci
      - run: npm run lint
      - run: npm test
      - run: npm run build
      - name: Commit validated Sprint 12
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/game-engine/season.ts src/app/page.tsx src/app/club-management-view.tsx next.config.ts
          if ! git diff --cached --quiet; then
            git commit -m "feat: connect Sprint 12 saves leagues and smart calendar"
            git push origin HEAD:feat/sprint12-multisave-calendar-leagues
          fi
