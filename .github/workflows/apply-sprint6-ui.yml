name: Apply Sprint 6 UI
on:
  push:
    branches: [feat/sprint6-market-contracts]
    paths:
      - scripts/apply-sprint6-ui.mjs
      - .github/workflows/apply-sprint6-ui.yml
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    timeout-minutes: 3
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/sprint6-market-contracts
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: node scripts/apply-sprint6-ui.mjs
      - name: Commit UI integration
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/app/page.tsx
          if git diff --cached --quiet; then exit 0; fi
          git commit -m "feat: connect transfer market to dashboard"
          git push origin HEAD:feat/sprint6-market-contracts
